baseURL = "https://vc24902.github.io/aws-worklog/"
languageCode = "en-us"
title = "AWS Worklog"
theme = "ananke"

canonifyURLs = true

[outputs]
home = ["HTML"]

disableKinds = ["RSS"]

[params]
mainSections = ["posts"]
customCSS = ["css/style.css"]

# Cho phép HTML trong Markdown
[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true